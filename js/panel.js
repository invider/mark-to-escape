this['@dna/panel'] = function() {

	return new $.sys.Frame({
		name : 'panel',

		evo: function() {
			//this._.env.player.currentMark = constants.keyCodes.SPAWN_MARK_LEFT
		},
		
		draw : function(ctx) {
		}
	})
}